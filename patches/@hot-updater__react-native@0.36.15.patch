diff --git a/android/build.gradle b/android/build.gradle
index df935bca1944bf483523d4617879f73d7160aa90..45fde053d9f271cdb60556d47109e1068ae2dc76 100644
--- a/android/build.gradle
+++ b/android/build.gradle
@@ -1,20 +1,3 @@
-buildscript {
-  // Buildscript is evaluated before everything else so we can't use getExtOrDefault
-  def kotlin_version = rootProject.ext.has("kotlinVersion") ? rootProject.ext.get("kotlinVersion") : project.properties["HotUpdater_kotlinVersion"]
-
-  repositories {
-    google()
-    mavenCentral()
-  }
-
-  dependencies {
-    classpath "com.android.tools.build:gradle:7.2.1"
-    // noinspection DifferentKotlinGradleVersion
-    classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
-
-  }
-}
-
 def reactNativeArchitectures() {
   def value = rootProject.getProperties().get("reactNativeArchitectures")
   return value ? value.split(",") : ["armeabi-v7a", "x86", "x86_64", "arm64-v8a"]
@@ -25,7 +8,6 @@ def isNewArchitectureEnabled() {
 }
 
 apply plugin: "com.android.library"
-apply plugin: "kotlin-android"
 apply from: "$projectDir/react-native-helpers.gradle"
 
 if (isNewArchitectureEnabled()) {
@@ -110,6 +92,9 @@ android {
     main {
       if (!isNewArchitectureEnabled()) {
         java.srcDirs += ["src/oldarch"]
+        // Kotlin sources are not picked up from java.srcDirs; add them
+        // explicitly so the arch-specific Kotlin files compile.
+        kotlin.srcDirs += ["src/oldarch"]
       } else {
         java.srcDirs += [
           "src/newarch",
@@ -117,6 +102,7 @@ android {
           "generated/java",
           "generated/jni"
         ]
+        kotlin.srcDirs += ["src/newarch"]
       }
     }
   }
